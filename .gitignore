# Mac OS files
*.DS_Store

# IntelliJ files
*.idea

# Ignore things if I run Behat outside of VM
tests/behat/behat.local.yml
vendor*
bin*
composer.lock

# Ignore libraries folder so assets aren't bundled with the code.
libraries/*
